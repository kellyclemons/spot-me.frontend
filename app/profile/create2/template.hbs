<div class="section">
  <div class="availability">
    <h2 class="availability-text">Select Availability:</h2>
    <div class="availability-options">

      {{#each availabilities as |availability|}}
        <div {{action "chooseAvailability" availability}} class="availability-options__item {{if (contains availability choices) "availability-options__item--selected"}}">
          <p>{{availability}}</p>
        </div>
      {{/each}}

    </div>

    <div class="move-on">
      <div class="move-on__button direction">{{#link-to 'profile.create1'}}<i class="fa fa-chevron-circle-left backward" aria-hidden="true"></i>{{/link-to}}</div>
      <div class="move-on__button skip">{{#link-to 'profile.create3'}}Skip{{/link-to}}</div>
      <div class="move-on__button direction">{{#link-to 'profile.create3'}}<i class="fa fa-chevron-circle-right forward" aria-hidden="true"></i>{{/link-to}}</div>
    </div>

  </div>
</div>
